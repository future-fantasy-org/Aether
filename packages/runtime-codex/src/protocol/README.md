Snapshot of `codex app-server generate-ts` output (codex-cli 0.144.1).
Regenerate with: codex app-server generate-ts --out packages/runtime-codex/src/protocol
Used as type documentation only; the adapter treats wire data as unknown and
validates structurally in mapping.ts.
